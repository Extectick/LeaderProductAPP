# ВАЖНО:
# Все EXPO_PUBLIC_* значения попадают в клиентскую сборку (web/android/ios).
# Не храните здесь приватные секреты.

# API
# Базовый URL для всех запросов приложения (axios/fetch).
EXPO_PUBLIC_API_URL_DEV=https://your-api-domain.com

# Провайдеры авторизации
# Username Telegram-бота для Telegram-входа (без @).
EXPO_PUBLIC_TELEGRAM_BOT_USERNAME=YourBotUsername
# Username Max-бота для Max-входа (без @).
EXPO_PUBLIC_MAX_BOT_USERNAME=YourBotUsername

# Обновления приложения
# Канал обновлений для UpdateGate/update-сервисов. Обычно: prod, dev.
EXPO_PUBLIC_UPDATE_CHANNEL=prod

# Проверки админ-доступа
# Основной permission-ключ для проверки прав администратора (см. hooks/useIsAdmin.ts).
EXPO_PUBLIC_ADMIN_PERMISSION=admin.access
# Резервное имя роли администратора, если permission-проверка недоступна.
EXPO_PUBLIC_ADMIN_ROLE=admin

# Мониторинг (Sentry)
# Включение/выключение инициализации Sentry в рантайме.
EXPO_PUBLIC_SENTRY_ENABLED=false
# DSN Sentry (обязателен только если EXPO_PUBLIC_SENTRY_ENABLED=true).
EXPO_PUBLIC_SENTRY_DSN=
# Тег окружения Sentry (production/staging/development).
EXPO_PUBLIC_SENTRY_ENVIRONMENT=production
# Необязательный идентификатор релиза (пример: leaderproduct@0.1.1+3).
EXPO_PUBLIC_SENTRY_RELEASE=
